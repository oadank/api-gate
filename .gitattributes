# 换行符策略 —— 关键：bin/ 下的无扩展名 sh 包装脚本必须是 LF。
# 本机 core.autocrlf=true，若不锁，checkout 时会把 #!/bin/sh 变成 #!/bin/sh\r 导致脚本直接报废。
* text=auto eol=lf

# Windows 批处理必须 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制
*.png binary
*.jpg binary
*.ico binary
